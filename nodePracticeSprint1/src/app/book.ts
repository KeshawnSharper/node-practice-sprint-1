import { FormControl,FormGroup } from '@angular/forms';

export interface Book {
    title: string,
    author: string,
    price: number,
    subtitle: string,
    description: string,
    published:string,
    website:string,
    pages:number,
    id:number
}
